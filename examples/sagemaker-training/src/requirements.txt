# Installed inside the training container before train.py runs.
# The base PyTorch DLC already has torch/torchvision.
3lc-ultralytics
# Uncomment for Apple Silicon local mode: default polars uses AVX/AVX2
# which Rosetta doesn't emulate, causing SIGILL. Not needed on real x86.
# polars[rtcompat]
